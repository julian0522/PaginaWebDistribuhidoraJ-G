---
import Layout from "../layouts/Layout.astro";

title: "Trabaja con Nosotros";
---

<Layout>
    <section class="relative py-20 px-4 bg-[url('/public/imagentrabajador.webp')] bg-no-repeat bg-center bg-fixed h-screen overflow-hidden">
         <!-- Overlay verde con opacidad -->
    <div class="absolute inset-0 bg-primary-50 opacity-65 z-10"></div>


    <div class="relative max-w-3xl mx-auto text-center z-20">
        <h1 class="text-5xl text-secondary-100 mb-4 font-extrabold">
          Trabaja con nosotros
        </h1>
        <p class="mb-8 text-secondary-100 text-xl">
            Si te interesa unirte a nuestro grupo de trabajo y construir junto a nosotros el campo colombiano escribemos y envianos tu hoja de vida
        </p>
  
        <form
          action="https://formsubmit.co/"
          method="POST"
          class="grid gap-6 text-left"
        >
          <div>
            <label
              for="nombre"
              class="text-lg block mb-1 font-medium text-white bg-secondary-100 rounded-3xl w-45 pl-3"
            >
              Nombre
            </label>
            <input
              id="nombre"
              name="nombre"
              type="text"
              class="w-full border border-gray-300 p-3 rounded-xl"
              required
            />
          </div>
  
          <div>
            <label
              for="email"
              class="text-lg block mb-1 font-medium text-white bg-secondary-100 rounded-3xl w-45 pl-3"
            >
              Correo electrónico
            </label>
            <input
              id="email"
              name="email"
              type="email"
              class="w-full border border-gray-300 p-3 rounded-xl"
              required
            />
          </div>
  
          <div>
            <label
              for="mensaje"
              class="text-lg block mb-1 font-medium text-white bg-secondary-100 rounded-3xl w-45 pl-3"
            >
              Hoja de Vida
            </label>
            <textarea
              id="mensaje"
              name="mensaje"
              rows="5"
              class="w-full border border-gray-300 p-3 rounded-xl"
              required></textarea>
          </div>
  
          <button
            type="submit"
            class=" text-lg bg-secondary-50 text-white py-3 px-6 rounded-xl hover:bg-secondary-100 w-45"
          >
            Enviar mensaje
          </button>
        </form>
      </div>
    </section>
</Layout>