---
const { bgClass = "bg-white", bgtext = "text-secondary-100" } = Astro.props;
---

<section class={`${bgClass} py-16 px-10`}>
    <!-- Primera fila -->
    <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8 items-center px-4 transform duration-600 md:hover:translate-x-4 md:hover:-translate-y-4 pointer-events-auto">
      <!-- Imagen -->
      <div class="relative rounded-3xl shadow-2xl overflow-hidden h-[300px] order-1 md:order-none">
        <img
          src="/public/fotocampo.jpg"
          alt="Equipo trabajando"
          class="w-full h-full object-cover"
        />
      </div>
  
      <!-- Texto -->
      <div class="text-black space-y-4 order-2 md:order-none">
        <h2 class={`text-4xl font-bold ${bgtext}`}>Nuestra Misión</h2>
        <p class="text-2xl leading-relaxed text-justify">
          Ser reconocidos como el principal aliado para la agricultura, mejorando la productividad, preservando nuestro ecosistema y su entorno.
        </p>
      </div>
    </div>
  
    <!-- Segunda fila -->
    <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8 items-center mt-16 px-4 transform duration-600 md:hover:translate-x-4 md:hover:-translate-y-4 pointer-events-auto">
      <!-- Texto -->
      <div class="text-black space-y-4 order-2 md:order-none">
        <h2 class={`text-4xl font-bold ${bgtext}`}>Nuestra Visión</h2>
        <p class="text-2xl leading-relaxed text-justify">
          Ser la distribuidora más amigable con el medio ambiente para preservar nuestros recursos.
        </p>
      </div>
  
      <!-- Imagen -->
      <div class="relative rounded-3xl shadow-2xl overflow-hidden h-[300px] order-1 md:order-none">
        <img
          src="/public/fotomercado.jpg"
          alt="Equipo trabajando"
          class="w-full h-full object-cover"
        />
      </div>
    </div>
  </section>